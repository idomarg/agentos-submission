<div dir="rtl">

# Decisions — OurMoney (example)

_החלטות נעולות לדוגמה. ערכים מייצגים._

---

## 2026-01-XX — מנוע הקטגוריזציה רץ offline, לא בתוך האפליקציה

- **מה הוחלט**: אין LLM בתוך האפליקציה. Claude מכין הצעות סיווג דרך Supabase MCP ומעלה אותן לטבלת `category_suggestions`; המשתמש מאשר/דוחה במסך `/categorize`.
- **למה**: מוציא עלות ו-latency ממסלול הבקשה, ושומר אדם בלולאת ההחלטה. סיווג הוא החלטה רגישה — לא רוצים אוטומציה עיוורת.
- **השלכות**: אסור לכתוב ישירות לטבלת `transactions` מתוך הסוכן. כל סיווג עובר דרך מסך אישור.
- **סטטוס**: 🔒 locked

---

## 2026-01-XX — שתי connections של MCP, שני תפקידים

- **מה הוחלט**: `postgres-readonly` (SELECT בלבד) לקריאה, ו-`supabase` (service-role) הוא המסלול היחיד שמותר לו לכתוב הצעות.
- **למה**: הפרדת הרשאות — קריאה לא יכולה בטעות לכתוב; כתיבה מחויבת לסנן `user_id` מפורש.
- **השלכות**: כל כתיבה דרך service-role חייבת `user_id` מפורש (אין `auth.uid()` שמגן).
- **סטטוס**: 🔒 locked

---

## 2026-06-XX — Vercel git-connected, בלי deploy ידני

- **מה הוחלט**: hosting דרך Vercel מחובר ל-git. push ל-`main` = production deploy.
- **למה**: CI/CD פשוט, מקור אמת אחד (git), בלי צעדים ידניים שאפשר לשכוח.
- **השלכות**: לא מריצים `vercel --prod` ידנית. ה-build המקומי הירוק הוא אות האיכות.
- **סטטוס**: 🔒 locked

</div>
