# Changelog

_Running log of changes made to this project. Updated by the agent at the end of each meaningful session._
_Read by the next agent to understand what has changed and which files are "hot"._

---

## Format

| Date | Change | Related Linear issue | Branch / PR | Files affected | Verification performed |
|---|---|---|---|---|---|
| YYYY-MM-DD | Plain-English description of what changed | IDO-X | `branch-name` / PR #N | `path/to/file1`, `path/to/file2` | e.g. "tests green, manually verified signup flow" |

---

## Log

| Date | Change | Related Linear issue | Branch / PR | Files affected | Verification performed |
|---|---|---|---|---|---|
| 2026-06-04 | IDO-101: Data freshness boundary — stop treating import data as real-time. The app is import-driven (data only as fresh as the last Riseup export), but several spots computed "now" from `new Date()`/`current_date`, so stale data looked live (worst case: after a month rolls over the dashboard opened on an **empty** calendar month). New `getDataFreshness()` derives a single as-of boundary from the data: `latestMonth` (max `cashflow_month` with txns) + `asOfDate` (max `payment_date`). Dashboard + budgets now default to the latest month **with data**; `v_upcoming_payments_30d` (migration 0006) computes its 30-day window from each user's `max(payment_date)` not `current_date`; subscriptions "overdue" measured vs `asOfDate − 7d`; new freshness banner ("נתונים נכונים ל-X" + age + ייבא-עדכון link) on dashboard/budgets; labels fixed ("תזרים נוכחי"→"תזרים החודש", "30 ימים הקרובים"→"30 יום מהנתונים האחרונים", landing "בזמן אמת" removed). | IDO-101 | `idomarg/ido-101-data-freshness-…` / PR #21 | `lib/freshness.ts`, `components/data-freshness-banner.tsx`, `supabase/migrations/0006_freshness_upcoming_payments.sql`, `app/dashboard/{page,_client}.tsx`, `app/budgets/{page,_client}.tsx`, `app/subscriptions/{page,_client}.tsx`, `app/page.tsx` | typecheck ✓, `npm run build` ✓; migration applied live + view verified via MCP (as_of=2026-05-31, 14 rows spanning 05-31…06-14; the 05-31 row sits before today 06-04 and would have been dropped under the old `current_date` window) |
| 2026-06-04 | IDO-100: Enriched `/categorize` suggestion cards. Each card now shows **2 sample transactions** (date · raw merchant · amount), an **amount-statistics strip** (min/max/avg/median/stddev over `abs(amount)` so you can tell a tight cluster from a wide range), and an editable **note** field saved to the suggestion (Claude reads it next round). Header shows a **pending counter**; pending list can be **filtered by suggested category**. Migration 0005 adds `category_suggestions.note` and extends `v_uncategorized_by_merchant` (CREATE OR REPLACE, columns appended) with the stats + a 2-row `sample_transactions` jsonb array. New `updateSuggestionNote` server action; `listSuggestions` merges live stats/samples by `group_key`. CLAUDE.md recipe updated to read the stats columns + reviewer notes before re-staging. | IDO-100 | `idomarg/ido-100-enrich-suggestion-cards` / PR #20 | `supabase/migrations/0005_suggestion_note_and_stats.sql`, `app/categorize/actions.ts`, `app/categorize/_client.tsx`, `CLAUDE.md` | typecheck ✓, `npm run build` ✓; migration applied live + view output spot-checked (paybox wide range vs אבשק מרקט tight cluster) |
| 2026-06-04 | IDO-99: `/categorize` fixes — (1) bug: category/business Select triggers showed the raw UUID; fixed by passing the resolved name as `SelectValue` children (Radix can't derive the label before `SelectContent` mounts). (2) feature: new `hidden` suggestion status — per-row **הסתר** parks a suggestion without rejecting; **הצג מוסתרים (N)** toggle reveals hidden rows with a **החזר** (unhide) action; hidden are excluded from "אשר הכל". `listSuggestions` now returns pending+hidden, client splits by status. Migration 0004 extends the `status` CHECK constraint to allow `hidden`. | IDO-99 | PR #19 → `dc6bf54` | `app/categorize/_client.tsx`, `app/categorize/actions.ts`, `supabase/migrations/0004_category_suggestions_hidden_status.sql` | typecheck ✓, `npm run build` ✓ (/categorize compiled), lint clean on touched files; migration applied live + constraint re-verified (`pg_constraint` now lists `hidden`); subagent review PASS on all 5 AC; Vercel preview SUCCESS |
| 2026-06-04 | Operational: ran the AI-assisted categorization loop (CLAUDE.md recipe). Staged **172** per-merchant suggestions into `category_suggestions` (all groups with ≥3 txns), covering **2,090** of 4,110 txns (76% of the 2,736 already-normalized). Confidence-graded with Hebrew reasoning; low-confidence P2P/ATM/unidentified groups flagged "לאימות". Awaiting user review at `/categorize`. | — (operational) | MCP staging only | `category_suggestions` (data) | All 172 group_keys matched the view (join-from-view staging); counts verified post-insert. Remaining: 1,291 un-normalized txns (need the backfill button) + ~547 long-tail 1–2-tx groups. |
| 2026-06-04 | IDO-55: Completed `docs/example-queries.md` — expanded the 7-query skeleton to all **19 Appendix-B acceptance queries** (PRD), grouped per the PRD. Fixed sign convention (expenses are negative → spend queries use `-amount` / `amount < 0 AND NOT is_excluded`; old skeleton's `amount > 200` matched income). Corrected `v_tithe_status` usage (per `cashflow_month`, no `year` col). Documented the session prefix `SELECT * FROM v_active_financial_context` + sign/cashflow_month conventions. Query #13 (house in 5 years) demonstrates `v_active_financial_context`. Queries 14–19 (investments) marked deferred until IDO-63. | IDO-55 | PR #18 → `71b68b2` | `docs/example-queries.md` | Every query executed against the live DB via Supabase MCP (active queries clean; category/merchant-joined return 0 rows pending categorization; 14–19 empty pending investment data — all documented). Subagent review: pass. Docs-only — no code/build impact. |
| 2026-06-03 | IDO-84: Documented the AI-assisted categorization workflow in CLAUDE.md — new **## AI-assisted categorization** section: read-only (`postgres-readonly`/`readonly_claude`) vs service-role (`supabase`) MCP split, why service-role writes must filter `user_id` explicitly (bypasses RLS), the IDO-80 backfill prerequisite, and the 5-step flow with real IDs + a full `INSERT … ON CONFLICT (user_id, group_key) DO UPDATE` example, ending at review in `/categorize`. Engine half = Claude via MCP (no in-app LLM, locked decision). | IDO-84 | PR #15 → `0506ded` | `CLAUDE.md` | Engine-half smoke-tested live via MCP (staged a suggestion → confirmed → deleted, 0 rows remaining); confirmed all 4110 txns `merchant_normalized IS NULL` (view empty, proves backfill note); `readonly_claude` SELECT policy present. Docs-only; UI-half (render→approve) deferred to IDO-83 AC. Inline self-review (docs-only). |
| 2026-06-03 | IDO-83: Categorize review screen `/categorize` — lists pending `category_suggestions` per merchant; approve applies category (+ optional business) to all still-uncategorized txns of that merchant via reused `bulkUpdateCategory`, optionally persists a rule via reused `createRule`; reject; "אשר הכל" bulk (skips category-less, reports skipped); seed + backfill trigger buttons; category/business names via PostgREST embeds; Zod + Hebrew messages. Nav link added to `components/app-nav.tsx` (real global nav, not the AC-stated dashboard/_client.tsx). | IDO-83 | PR #16 → `cdadfae` | `app/categorize/{actions,page,_client,loading}.tsx`, `components/app-nav.tsx` | typecheck 0 errors, `npm run build` ok (/categorize compiled), lint clean for touched files, both FK embeds verified live; subagent pass-with-notes (non-atomic business update self-heals on retry, documented inline). Runtime approve untested — needs suggestion data from IDO-84 MCP workflow |
| 2026-06-03 | IDO-82: Migration 0003 — `category_suggestions` staging table (unique(user_id, group_key) for MCP upsert, index on (user_id, status), updated_at trigger, own-rows RLS + a `readonly_claude` select-all RLS policy so the read role bypasses the auth.uid() filter) + `v_uncategorized_by_merchant` view. readonly_claude granted SELECT on both. | IDO-82 | PR #12 | `supabase/migrations/0003_category_suggestions.sql` | Applied live via apply_migration + execute_sql; verified table shape, both RLS policies, unique/index/trigger, grants, view def; subagent pass |
| 2026-06-03 | IDO-81: Default Hebrew category tree — idempotent `seedDefaultCategories()` + `DEFAULT_CATEGORIES` (15 roots + sub-categories, lucide icons/hex colors, computed sort_order). Matches `(user_id, lower(name))` so re-runs create no duplicates. "צור עץ קטגוריות ברירת מחדל" button wired to `seedDefaultCategoriesAction`. | IDO-81 | PR #14 | `lib/categories/seed.ts`, `lib/categories/seed.test.ts`, `app/categories/actions.ts`, `app/categories/_client.tsx` | typecheck 0 errors, 37/37 tests (8 new), subagent pass-with-notes; icon→lucide rendering deferred to issue D |
| 2026-06-03 | IDO-80: Merchant normalization — deterministic, idempotent `normalizeMerchant()` populates `merchant_normalized` on import + manual create; `backfillMerchantNormalized()` for existing rows. Foundation for grouping merchants (categorize suggestions, subscriptions). | IDO-80 | PR #13 | `lib/merchants/normalize.ts`, `lib/merchants/normalize.test.ts`, `lib/riseup/importer.ts`, `app/transactions/actions.ts` | typecheck 0 errors, 41/41 tests, subagent pass-with-notes; dedup unaffected |
| 2026-06-01 | IDO-47: אימות ייבוא 4,110 תנועות מ-Riseup, מסומן Done | IDO-47 | operational | — | Screenshot: 4,110 imported, 0 errors, 19 duplicates detected |
| 2026-06-01 | IDO-48: CRUD screens — accounts, categories (3-level tree), businesses, tags + manage-nav | IDO-48 | PR #4 | `app/accounts/`, `app/categories/`, `app/businesses/`, `app/tags/`, `components/manage-nav.tsx` | TypeCheck clean, subagent review pass |
| 2026-06-01 | IDO-49: Transactions List — pagination, filters, edit panel, bulk categorize, "הפוך לחוק" | IDO-49 | PR #5 | `app/transactions/page.tsx`, `app/transactions/_client.tsx`, `app/transactions/actions.ts` | TypeCheck clean, subagent review pass-with-notes (all addressed) |
| 2026-06-01 | IDO-68: middleware.ts — auth guard, redirect unauthenticated to /login | IDO-68 | main/56e6018 | `middleware.ts` | typecheck clean |
| 2026-06-01 | IDO-69: AppNav global nav header + signOut server action | IDO-69 | main/56e6018 | `components/app-nav.tsx`, `app/actions.ts`, `app/layout.tsx` | typecheck clean |
| 2026-06-01 | IDO-70: Root layout — lang=he dir=rtl + Toaster mounted | IDO-70 | main/56e6018 | `app/layout.tsx` | typecheck clean |
| 2026-06-01 | IDO-71: Toast feedback on all mutations (5 _client files) | IDO-71 | main/56e6018 | `app/transactions/_client.tsx`, `app/businesses/_client.tsx`, `app/categories/_client.tsx`, `app/tags/_client.tsx`, `app/accounts/_client.tsx` | typecheck clean |
| 2026-06-01 | IDO-72: loading.tsx skeletons for transactions, dashboard, accounts, businesses, categories | IDO-72 | main/56e6018 | `app/*/loading.tsx` (5 files) | typecheck clean |
| 2026-06-01 | IDO-73: login Loader2 spinner + dashboard "כל התנועות" deep-link with ?month= | IDO-73 | main/56e6018 | `app/(auth)/login/page.tsx`, `app/dashboard/_client.tsx` | typecheck clean |
| 2026-06-01 | IDO-74: businesses delete + is_active toggle (actions + UI) | IDO-74 | main/56e6018 | `app/businesses/actions.ts`, `app/businesses/_client.tsx` | typecheck clean |
| 2026-06-01 | IDO-75: lib/constants.ts — shared ACCOUNT_TYPES, CATEGORY_TYPES, EXPENSE_KINDS | IDO-75 | main/56e6018 | `lib/constants.ts`, `app/accounts/_client.tsx`, `app/categories/_client.tsx` | typecheck clean |
| 2026-06-01 | IDO-76: import page — Loader2 spinner + step messages (parse + import phases) | IDO-76 | main/56e6018 | `app/import/page.tsx` | typecheck clean |
| 2026-06-01 | IDO-77: createTransaction action + "תנועה חדשה" Dialog in /transactions | IDO-77 | main/56e6018 | `app/transactions/actions.ts`, `app/transactions/page.tsx`, `app/transactions/_client.tsx` | typecheck clean |
| 2026-06-01 | IDO-78: Zod validation on accounts, businesses, categories, tags actions | IDO-78 | main/56e6018 | `app/accounts/actions.ts`, `app/businesses/actions.ts`, `app/categories/actions.ts`, `app/tags/actions.ts` | typecheck clean |
| 2026-06-01 | IDO-79: parser.test.ts + actions.test.ts — 20/20 tests pass | IDO-79 | main/56e6018 | `lib/riseup/parser.test.ts`, `app/transactions/actions.test.ts` | npm test: 20/20 green |
| 2026-06-02 | IDO-54: Business Tagging + Splits — default_business_id on categories, auto-apply after import, split dialog with validation, "פוצל" badge in list | IDO-54 | PR #11 | `app/categories/`, `app/transactions/SplitDialog.tsx`, `app/transactions/_client.tsx`, `app/transactions/actions.ts`, `lib/rules/apply.ts` | typecheck 0 errors, 29/29 tests, subagent review pass |
| 2026-06-02 | IDO-53: Zod validation for toggleRollover — added rolloverSchema, fixed user_id scope on updateCategory | IDO-53 | PR #10 | `app/budgets/actions.ts`, `app/categories/actions.ts` | typecheck clean |
| 2026-06-02 | IDO-51: Rules Engine — applyRules() with 6 operators, CRUD UI, auto-apply on import | IDO-51 | PR #9 / 04367bb | `lib/rules/`, `app/rules/`, `tests/rules-engine.test.ts`, `app/api/import/route.ts` | npm test: 29/29 green |
| 2026-06-02 | IDO-52: Subscription detection — v_subscription_candidates query, confidence score, /subscriptions CRUD screen | IDO-52 | PR #9 / 04367bb | `lib/subscriptions/`, `app/subscriptions/`, `components/app-nav.tsx` | npm test: 29/29 green |
| 2026-06-01 | IDO-50: Dashboard חודשי — 5 blocks, weekly chart, category chart, recent transactions | IDO-50 | PR #6 (In Review) | `app/dashboard/page.tsx`, `app/dashboard/_client.tsx` | TypeCheck clean — awaiting user approval to merge |
| 2026-06-01 | IMPROVEMENTS.md — סקירה מלאה של כל האפליקציה, 20+ ממצאים, 10 פעולות לעדיפות גבוהה | — | — | `IMPROVEMENTS.md` | New file |

---

_Newest entries at the top. One row per meaningful change (not per commit)._
_Keep "Change" descriptions user-readable — what can Ido now do that he couldn't before?_
