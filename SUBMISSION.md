<div dir="rtl">

# SUBMISSION — AgentOS

מסמך קצר לקריאה מהירה, בלי להיכנס לכל הקבצים.

---

## Scope

AgentOS הוא **לא** מוצר SaaS. זוהי שכבת התפעול האישית/פנימית שלי לעבודת פיתוח בעזרת AI. ההגשה מדגימה Workflow Overlay לשימוש חוזר: קבצי memory, פקודות, קונבנציות Linear, ודוגמאות אמיתיות מפרויקטים. הערך המרכזי הוא צמצום ההתערבות הידנית ב**ניהול הפרויקט התפעולי** (issues, branch, סטטוס, הקשר, המשכיות, תיאום סוכנים) — לא בהחלטות מוצר, סיכון או עסק, שנשארות אצל האדם.

---

## מה בניתי

**AgentOS** — שכבת עבודה (Workflow Overlay) לסוכני פיתוח מבוססי AI כמו Claude Code. אוסף של קבצי פרויקט קבועים, slash commands, דוגמאות hooks, וקונבנציית עבודה מול Linear — שיחד נותנים לסוכן **זיכרון מתמשך בין סשנים**.

---

## למה בניתי

עבדתי על פרויקטים אמיתיים מול Claude Code וראיתי שוב ושוב את אותה בעיה: כל סשן מתחיל מאפס. הסוכן שכח החלטות, חזר על טעויות, ולא ידע מה הסטטוס. במקום לפתור את זה מחדש בכל פרויקט — בניתי מסגרת חוזרת אחת.

---

## הבעיה שזה פותר

סוכני AI לא זוכרים הקשר בין סשנים. התוצאה: הסבר חוזר, החלטות שנשכחות, עבודה כפולה, וחוסר מקור אמת אחד. הכאב מתחדד כשעובדים על פני כמה ימים, כמה סשנים, או כמה סוכנים במקביל.

---

## איך זה עובד

הזיכרון של הסוכן הוא **קבצים ב-repo**, לא state פנימי:

- בתחילת סשן — `/prime` קורא את ההקשר (`CLAUDE.md`, `STANDUP.md`, `DECISIONS.md`).
- במהלך — העבודה מתנהלת מול issues ב-Linear, branch אחד למשימה.
- בסוף — `/wrap-up` מעדכן את הקבצים דרך "שער ניתוב בארבע שאלות" (החלטה? לקח? התקדמות? סטטוס?).

הכלל: **קוראים בהתחלה, כותבים בסוף.** מה שנכתב לקובץ — נשמר לסשן הבא.

---

## הוכחת שימוש / Case studies

שני פרויקטים אמיתיים שמשתמשים בשיטה (ראה [`examples/`](examples/)):

1. **OurMoney** — מוצר אישי לניהול תקציב והון אישי (Next.js + Supabase). פרויקט מורכב עם issues של ייבוא נתונים, מנוע קטגוריזציה מבוסס Claude/MCP, מנוע חוקים, וזיהוי מנויים. דוגמה ל-AgentOS בפרויקט מוצר מתגלגל.

2. **Nap Inventory** — מערכת מלאי לעסק פיצה/פודטראק. דוגמה ל-AgentOS בעסק קטן: ניהול מלאי, ספקים, סגירת יום, וביצוע מול Linear.

כל case study כולל `STATUS`, `DECISIONS`, `LEARNINGS`, `CHANGELOG` לדוגמה.

---

## מה הייתי ממשיך לבנות

- **Installer אוטומטי** — פקודה אחת שמתקינה את ה-Overlay ב-repo חדש במקום העתקה ידנית.
- **Hooks אמיתיים** — חיבור הדוגמאות לסביבת ההרצה של Claude Code, כך שהקשר נטען ונשמר אוטומטית.
- **אינטגרציית Linear API דו־כיוונית** — סנכרון אוטומטי מלא בין הקבצים ל-Linear.
- **שכבת Vault על־פרויקטלית** — זיכרון שחוצה פרויקטים (למשל דרך Obsidian).

---

## מה כבר עובד ומה עדיין prototype/template

> 🟢 **עובד בפועל (MVP):** קבצי ה-memory, ה-slash commands, וקונבנציית ה-Linear — מוכחים בשימוש בשני פרויקטים אמיתיים.
>
> 🟡 **Template / המחשה:** ה-hooks הם דוגמאות שמראות את הרעיון, לא מחוברות ל-API אמיתי.
>
> ⏳ **Future:** installer אוטומטי, hooks מחוברים, ואינטגרציית Linear API עמוקה.

זה לא מוצר production מותקן־בלחיצה. זו **מסגרת מוכחת בשימוש** עם מסלול ברור להמשך.

</div>
